{
  "name": "whoswho",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently --kill-others --kill-others-on-fail 'npm run dynamo' 'npm run json:server' 'npm run watch:dev'" ,
    "watch:dev": "nodemon ./src/index.js",
    "dynamo": "java -Djava.library.path=~/dynamodb_local_latest/DynamoDBLocal_lib/ -jar ~/dynamodb_local_latest/DynamoDBLocal.jar",
    "json:server": "json-server --watch ./src/db/db.json",
    "create-db": "node ./src/db/createTable.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.0.0",
    "aws-sdk": "^2.286.2",
    "axios": "^0.18.0",
    "graphql": "^0.13.2"
  },
  "devDependencies": {
    "concurrently": "^3.6.1",
    "json-server": "^0.14.0",
    "nodemon": "^1.18.3"
  }
}
